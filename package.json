{
    "name": "ivy_db",
    "version": "1.0.0",
    "scripts": {
        "dev": "npx nodemon --exec python3 index.py",
        "api": "npx nodemon --exec python3 server.py",
        "test": "npx nodemon --exec python3 test.py",
        "wintest": "npx nodemon --exec python test.py",
        "winapi": "npx nodemon --exec python server.py"
    },
    "dependencies": {
        "nodemon": "^2.0.14"
    },
    "type": "module",
    "nodemonConfig": {
        "watch": [
            "*.py"
        ],
        "ignore": [
            "/resource/*"
        ],
        "delay": 1,
        "execMap": {
            "pl": "perl"
        },
        "ext": "py"
    }
}
